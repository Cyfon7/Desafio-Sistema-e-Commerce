<%= form_with(model: item_attribute, local: true) do |form| %>
  <% if item_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_attribute.errors.count, "error") %> prohibited this item_attribute from being saved:</h2>

      <ul>
      <% item_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
